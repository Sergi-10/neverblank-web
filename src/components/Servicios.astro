---
const columns = [
  {
    number: "01",
    title: "Estrategia",
    description:
      "Perfil de cliente, público objetivo, tono de marca y competencia — generado con un cuestionario guiado por IA, siempre editable. Define la base sobre la que se construye todo el contenido de cada cuenta.",
    icon: "clipboard",
  },
  {
    number: "02",
    title: "Contenido",
    description:
      "Calendario mensual completo por cliente: carrusel, reel, story o hilo, con copy, texto de cada diapositiva y hashtags ya pensados. Genera un mes entero de publicaciones en minutos, no en días.",
    icon: "calendar",
  },
  {
    number: "03",
    title: "Gestión de equipo",
    description:
      "Varios clientes, varios usuarios, roles de agencia. Todo el equipo trabajando sobre el mismo calendario, con estados de aprobación y revisión, sin salir de la plataforma.",
    icon: "users",
  },
  {
    number: "04",
    title: "Servicios",
    description:
      "Todo lo que necesitas para dejar de gestionar redes sociales a mano. Una sola herramienta que sustituye el Word, el Excel, el WhatsApp y los prompts sueltos.",
    icon: "gear",
  },
];
---

<section id="servicios" class="scroll-mt-16 bg-brand-bg py-24 mt-16">
  <div class="mx-auto max-w-6xl px-6">
    <span class="text-sm font-semibold uppercase tracking-widest text-brand-navy/50">
      Servicios
    </span>
    <h2 class="font-display mt-3 text-4xl font-bold text-brand-navy md:text-5xl">
      Todo lo que necesitas, nada que sobre
    </h2>

    <div class="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-4">
      {columns.map((col, i) => (
        <div class="flex flex-col rounded-xl border border-brand-navy/10 p-8 transition-shadow hover:shadow-lg">
          <div class="flex items-start justify-between">
            {col.icon === "clipboard" && (
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="h-10 w-10 text-brand-orange">
                <rect x="6" y="4" width="12" height="17" rx="1.5" />
                <path d="M9 4V3a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v1" />
                <path d="M9 12l2 2 4-4" />
              </svg>
            )}
            {col.icon === "calendar" && (
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="h-10 w-10 text-brand-orange">
                <rect x="3.5" y="5" width="17" height="15" rx="1.5" />
                <path d="M3.5 9.5h17" />
                <path d="M8 3v3M16 3v3" />
                <path d="M8 13.5h.01M12 13.5h.01M16 13.5h.01M8 16.5h.01M12 16.5h.01M16 16.5h.01" />
              </svg>
            )}
            {col.icon === "users" && (
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="h-10 w-10 text-brand-orange">
                <circle cx="9" cy="8" r="3" />
                <path d="M3.5 20c0-3.31 2.46-6 5.5-6s5.5 2.69 5.5 6" />
                <circle cx="17" cy="9" r="2.5" />
                <path d="M15.5 14.2c2.53.4 4.5 2.66 4.5 5.8" />
              </svg>
            )}
            {col.icon === "gear" && (
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="h-10 w-10 text-brand-orange">
                <circle cx="12" cy="12" r="3" />
                <path d="M12 3v2.2M12 18.8V21M21 12h-2.2M5.2 12H3M18.2 5.8l-1.55 1.55M7.35 16.65 5.8 18.2M18.2 18.2l-1.55-1.55M7.35 7.35 5.8 5.8" />
              </svg>
            )}
            <span class="font-display text-4xl font-extrabold text-brand-navy/10">{col.number}</span>
          </div>

          <h3 class="mt-6 text-xl font-bold text-brand-navy">{col.title}</h3>
          <p class="mt-3 text-base leading-relaxed text-brand-navy/70">{col.description}</p>

          {i === 3 && (
            <a
              href="#precios"
              class="mt-4 inline-flex items-center gap-1 text-lg font-semibold text-brand-orange transition-all hover:gap-2 hover:underline"
            >
              Ver precios <span aria-hidden="true">→</span>
            </a>
          )}
        </div>
      ))}
    </div>
  </div>
</section>
